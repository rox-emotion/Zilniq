diff --git a/node_modules/@kayzmann/expo-healthkit/ios/ExpoHealthKitManager.swift b/node_modules/@kayzmann/expo-healthkit/ios/ExpoHealthKitManager.swift
index a787e92..07df18f 100644
--- a/node_modules/@kayzmann/expo-healthkit/ios/ExpoHealthKitManager.swift
+++ b/node_modules/@kayzmann/expo-healthkit/ios/ExpoHealthKitManager.swift
@@ -142,6 +142,55 @@ class ExpoHealthKitManager {
     }
   }
 
+  func getActiveEnergyBurned(startDate: Date, endDate: Date) async throws -> Double {
+    return try await getCumulativeKilocalories(
+      identifier: .activeEnergyBurned, startDate: startDate, endDate: endDate)
+  }
+
+  func getBasalEnergyBurned(startDate: Date, endDate: Date) async throws -> Double {
+    return try await getCumulativeKilocalories(
+      identifier: .basalEnergyBurned, startDate: startDate, endDate: endDate)
+  }
+
+  private func getCumulativeKilocalories(
+    identifier: HKQuantityTypeIdentifier, startDate: Date, endDate: Date
+  ) async throws -> Double {
+    let predicate = HKQuery.predicateForSamples(withStart: startDate, end: endDate, options: .strictStartDate)
+
+    return try await withCheckedThrowingContinuation { continuation in
+      let query = HKStatisticsQuery(
+        quantityType: HKQuantityType.quantityType(forIdentifier: identifier)!,
+        quantitySamplePredicate: predicate,
+        options: .cumulativeSum
+      ) { _, result, error in
+        if let error = error {
+          let nsError = error as NSError
+          // HealthKit deliberately reports read-permission denial as "no data".
+          // Mirror that for these best-effort energy reads instead of surfacing
+          // an error the UI can't act on. Real failures still propagate.
+          let benign: Set<Int> = [
+            HKError.Code.errorAuthorizationDenied.rawValue,
+            HKError.Code.errorAuthorizationNotDetermined.rawValue,
+            HKError.Code.errorNoData.rawValue,
+          ]
+          if nsError.domain == HKError.errorDomain && benign.contains(nsError.code) {
+            continuation.resume(returning: 0)
+          } else {
+            continuation.resume(
+              throwing: HealthKitError.queryFailed(
+                "\(nsError.domain) \(nsError.code): \(nsError.localizedDescription)"))
+          }
+          return
+        }
+
+        let kcal = result?.sumQuantity()?.doubleValue(for: .kilocalorie()) ?? 0
+        continuation.resume(returning: kcal)
+      }
+
+      healthStore.execute(query)
+    }
+  }
+
   func deleteWorkout(id: String) async throws {
     guard let uuid = UUID(uuidString: id) else {
       throw HealthKitError.missingRequiredData("Invalid workout ID")
@@ -402,8 +451,10 @@ class ExpoHealthKitManager {
     )
   }
 
-  func saveProtein(grams: Double, date: Date) async throws {
-    try await saveQuantitySample(
+  /// Returns the UUID of the saved sample, so callers can keep it around to
+  /// identify (and later delete) the exact entry this wrote to Health.
+  func saveProtein(grams: Double, date: Date) async throws -> String {
+    return try await saveQuantitySample(
       identifier: .dietaryProtein,
       value: grams,
       unit: .gram(),
@@ -411,8 +462,8 @@ class ExpoHealthKitManager {
     )
   }
 
-  func saveCarbs(grams: Double, date: Date) async throws {
-    try await saveQuantitySample(
+  func saveCarbs(grams: Double, date: Date) async throws -> String {
+    return try await saveQuantitySample(
       identifier: .dietaryCarbohydrates,
       value: grams,
       unit: .gram(),
@@ -420,8 +471,8 @@ class ExpoHealthKitManager {
     )
   }
 
-  func saveFat(grams: Double, date: Date) async throws {
-    try await saveQuantitySample(
+  func saveFat(grams: Double, date: Date) async throws -> String {
+    return try await saveQuantitySample(
       identifier: .dietaryFatTotal,
       value: grams,
       unit: .gram(),
@@ -429,6 +480,15 @@ class ExpoHealthKitManager {
     )
   }
 
+  func saveEnergy(kilocalories: Double, date: Date) async throws -> String {
+    return try await saveQuantitySample(
+      identifier: .dietaryEnergyConsumed,
+      value: kilocalories,
+      unit: .kilocalorie(),
+      date: date
+    )
+  }
+
   // MARK: - Generic Helper Methods
 
   private func getQuantitySum(identifier: HKQuantityTypeIdentifier, startDate: Date, endDate: Date, unit: HKUnit) async throws -> Double {
@@ -518,7 +578,8 @@ class ExpoHealthKitManager {
     }
   }
 
-  private func saveQuantitySample(identifier: HKQuantityTypeIdentifier, value: Double, unit: HKUnit, date: Date) async throws {
+  @discardableResult
+  private func saveQuantitySample(identifier: HKQuantityTypeIdentifier, value: Double, unit: HKUnit, date: Date) async throws -> String {
     let quantity = HKQuantity(unit: unit, doubleValue: value)
     let sample = HKQuantitySample(
       type: HKQuantityType.quantityType(forIdentifier: identifier)!,
@@ -529,6 +590,7 @@ class ExpoHealthKitManager {
 
     do {
       try await healthStore.save(sample)
+      return sample.uuid.uuidString
     } catch {
       throw HealthKitError.saveFailed(error.localizedDescription)
     }
diff --git a/node_modules/@kayzmann/expo-healthkit/ios/ExpoHealthKitModule.swift b/node_modules/@kayzmann/expo-healthkit/ios/ExpoHealthKitModule.swift
index 6afa551..54caec1 100644
--- a/node_modules/@kayzmann/expo-healthkit/ios/ExpoHealthKitModule.swift
+++ b/node_modules/@kayzmann/expo-healthkit/ios/ExpoHealthKitModule.swift
@@ -45,6 +45,20 @@ public class ExpoHealthKitModule: Module {
       )
     }
 
+    AsyncFunction("getActiveEnergyBurned") { (startDate: Double, endDate: Double) async throws -> Double in
+      return try await healthManager.getActiveEnergyBurned(
+        startDate: Date(timeIntervalSince1970: startDate),
+        endDate: Date(timeIntervalSince1970: endDate)
+      )
+    }
+
+    AsyncFunction("getBasalEnergyBurned") { (startDate: Double, endDate: Double) async throws -> Double in
+      return try await healthManager.getBasalEnergyBurned(
+        startDate: Date(timeIntervalSince1970: startDate),
+        endDate: Date(timeIntervalSince1970: endDate)
+      )
+    }
+
     AsyncFunction("deleteWorkout") { (workoutId: String) async throws in
       try await healthManager.deleteWorkout(id: workoutId)
     }
@@ -155,19 +169,24 @@ public class ExpoHealthKitModule: Module {
       try await healthManager.saveCaffeine(milligrams: milligrams, date: date)
     }
 
-    AsyncFunction("saveProtein") { (grams: Double, timestamp: Double?) async throws in
+    AsyncFunction("saveProtein") { (grams: Double, timestamp: Double?) async throws -> String in
+      let date = timestamp != nil ? Date(timeIntervalSince1970: timestamp!) : Date()
+      return try await healthManager.saveProtein(grams: grams, date: date)
+    }
+
+    AsyncFunction("saveCarbs") { (grams: Double, timestamp: Double?) async throws -> String in
       let date = timestamp != nil ? Date(timeIntervalSince1970: timestamp!) : Date()
-      try await healthManager.saveProtein(grams: grams, date: date)
+      return try await healthManager.saveCarbs(grams: grams, date: date)
     }
 
-    AsyncFunction("saveCarbs") { (grams: Double, timestamp: Double?) async throws in
+    AsyncFunction("saveFat") { (grams: Double, timestamp: Double?) async throws -> String in
       let date = timestamp != nil ? Date(timeIntervalSince1970: timestamp!) : Date()
-      try await healthManager.saveCarbs(grams: grams, date: date)
+      return try await healthManager.saveFat(grams: grams, date: date)
     }
 
-    AsyncFunction("saveFat") { (grams: Double, timestamp: Double?) async throws in
+    AsyncFunction("saveEnergy") { (kilocalories: Double, timestamp: Double?) async throws -> String in
       let date = timestamp != nil ? Date(timeIntervalSince1970: timestamp!) : Date()
-      try await healthManager.saveFat(grams: grams, date: date)
+      return try await healthManager.saveEnergy(kilocalories: kilocalories, date: date)
     }
   }
 }
diff --git a/node_modules/@kayzmann/expo-healthkit/src/ExpoHealthKit.ts b/node_modules/@kayzmann/expo-healthkit/src/ExpoHealthKit.ts
index df24c83..2503ef6 100644
--- a/node_modules/@kayzmann/expo-healthkit/src/ExpoHealthKit.ts
+++ b/node_modules/@kayzmann/expo-healthkit/src/ExpoHealthKit.ts
@@ -99,6 +99,36 @@ export async function getTotalCalories(
   return await ExpoHealthKitModule.getTotalCalories(start, end);
 }
 
+/**
+ * Get total active energy burned (Apple Health "Active Energy") for a date range
+ * @param startDate - Start date
+ * @param endDate - End date
+ * @returns Active energy burned in kilocalories
+ */
+export async function getActiveEnergyBurned(
+  startDate: Date,
+  endDate: Date
+): Promise<number> {
+  const start = startDate.getTime() / 1000;
+  const end = endDate.getTime() / 1000;
+  return await ExpoHealthKitModule.getActiveEnergyBurned(start, end);
+}
+
+/**
+ * Get total basal (resting) energy burned (Apple Health "Resting Energy") for a date range
+ * @param startDate - Start date
+ * @param endDate - End date
+ * @returns Basal energy burned in kilocalories
+ */
+export async function getBasalEnergyBurned(
+  startDate: Date,
+  endDate: Date
+): Promise<number> {
+  const start = startDate.getTime() / 1000;
+  const end = endDate.getTime() / 1000;
+  return await ExpoHealthKitModule.getBasalEnergyBurned(start, end);
+}
+
 /**
  * Delete a workout from HealthKit
  * @param workoutId - The UUID of the workout to delete
@@ -336,8 +366,9 @@ export async function saveCaffeine(milligrams: number, date?: Date): Promise<voi
  * Save protein intake to HealthKit
  * @param grams - Amount of protein in grams
  * @param date - Optional date (defaults to now)
+ * @returns The UUID of the saved sample
  */
-export async function saveProtein(grams: number, date?: Date): Promise<void> {
+export async function saveProtein(grams: number, date?: Date): Promise<string> {
   const timestamp = date ? date.getTime() / 1000 : undefined;
   return await ExpoHealthKitModule.saveProtein(grams, timestamp);
 }
@@ -346,8 +377,9 @@ export async function saveProtein(grams: number, date?: Date): Promise<void> {
  * Save carbohydrate intake to HealthKit
  * @param grams - Amount of carbs in grams
  * @param date - Optional date (defaults to now)
+ * @returns The UUID of the saved sample
  */
-export async function saveCarbs(grams: number, date?: Date): Promise<void> {
+export async function saveCarbs(grams: number, date?: Date): Promise<string> {
   const timestamp = date ? date.getTime() / 1000 : undefined;
   return await ExpoHealthKitModule.saveCarbs(grams, timestamp);
 }
@@ -356,8 +388,20 @@ export async function saveCarbs(grams: number, date?: Date): Promise<void> {
  * Save fat intake to HealthKit
  * @param grams - Amount of fat in grams
  * @param date - Optional date (defaults to now)
+ * @returns The UUID of the saved sample
  */
-export async function saveFat(grams: number, date?: Date): Promise<void> {
+export async function saveFat(grams: number, date?: Date): Promise<string> {
   const timestamp = date ? date.getTime() / 1000 : undefined;
   return await ExpoHealthKitModule.saveFat(grams, timestamp);
 }
+
+/**
+ * Save dietary energy (calories) intake to HealthKit
+ * @param kilocalories - Amount of energy in kilocalories
+ * @param date - Optional date (defaults to now)
+ * @returns The UUID of the saved sample
+ */
+export async function saveEnergy(kilocalories: number, date?: Date): Promise<string> {
+  const timestamp = date ? date.getTime() / 1000 : undefined;
+  return await ExpoHealthKitModule.saveEnergy(kilocalories, timestamp);
+}
diff --git a/node_modules/@kayzmann/expo-healthkit/src/index.ts b/node_modules/@kayzmann/expo-healthkit/src/index.ts
index b71f77b..76d2437 100644
--- a/node_modules/@kayzmann/expo-healthkit/src/index.ts
+++ b/node_modules/@kayzmann/expo-healthkit/src/index.ts
@@ -9,6 +9,8 @@ export {
   deleteWorkout,
   getTotalDistance,
   getTotalCalories,
+  getActiveEnergyBurned,
+  getBasalEnergyBurned,
 
   // Steps & Activity
   getSteps,
@@ -40,6 +42,7 @@ export {
   saveProtein,
   saveCarbs,
   saveFat,
+  saveEnergy,
 } from './ExpoHealthKit';
 
 export type {
